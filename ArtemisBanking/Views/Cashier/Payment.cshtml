@model Application.ViewModels.PaymentViewModel;
@{
    ViewData["Title"] = "Realizar Pago";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow-sm border-0">
                <div class="card-header bg-warning text-dark fw-bold">
                    <h4 class="mb-0"><i class="bi bi-credit-card"></i> Realizar Pago de Préstamo</h4>
                </div>
                <div class="card-body bg-light">
                    @if (!ViewData.ModelState.IsValid)
                    {
                        <div class="alert alert-danger fw-bold mb-3">
                            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                        </div>
                    }

                    <form asp-controller="Cashier" asp-action="Payment" method="post">
                        
                        <div class="mb-3">
                            <label asp-for="LoanId" class="form-label fw-bold">Número de Préstamo</label>
                            <input asp-for="LoanId" class="form-control" placeholder="Ej: 12345" />
                            <span asp-validation-for="LoanId" class="text-danger"></span>
                        </div>

                        <div class="mb-4">
                            <label asp-for="Amount" class="form-label fw-bold">Monto a abonar (RD$)</label>
                            <input asp-for="Amount" type="number" step="0.01" class="form-control" placeholder="0.00" />
                            <span asp-validation-for="Amount" class="text-danger"></span>
                        </div>

                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-warning text-dark fw-bold shadow-sm">Siguiente Paso</button>
                            <a asp-controller="Cashier" asp-action="Index" class="btn btn-outline-secondary">Cancelar</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>